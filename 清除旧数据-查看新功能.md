# 🔄 清除旧数据，查看新功能

> 如果看不到两级分组功能，可能需要清除旧的 LocalStorage 数据

---

## 🎯 快速清除方法

### 方法 1：浏览器控制台（最简单）

1. **打开浏览器**，访问应用页面
2. **按 F12** 打开开发者工具
3. **点击 "Console" 标签**
4. **输入以下命令**：

```javascript
// 清除所有 Risk Papers
localStorage.removeItem('riskgen_papers')

// 刷新页面
location.reload()
```

### 方法 2：使用数据管理工具

1. **访问**：`http://localhost:8000/02-tools/data-manager.html`
2. **点击 "只清空 Risk Papers"** 按钮
3. **返回主应用**，刷新页面

---

## 🔍 查看调试信息

清除数据后，刷新页面，打开浏览器控制台（F12），你应该看到：

### 1. 数据加载信息
```
📊 Demo Data Loaded: 2 risk items
✅ Demo risk data loaded: 2 risks
```

### 2. Paper 创建/更新信息
```
📝 Auto-loading demo data...
```
或
```
🔄 Updating existing paper with new fields...
```

### 3. 分组信息
```
🧭 QuickNav grouping: { id: '...', level1: 'Data Risk', level2: 'Data Governance', ... }
🧭 QuickNav grouped structure: { 'Data Risk': { 'Data Governance': [...] } }

📊 Grouping risk: { id: '...', level1: 'Data Risk', level2: 'Data Governance', ... }
📊 Grouped structure: { 'Data Risk': { 'Data Governance': [...] } }
```

---

## ✅ 验证新功能

清除数据并刷新后，你应该看到：

### Quick Navigation（左侧导航）
- ✅ 按 **Level 1** 分组（如 "Data Risk"）
- ✅ 每个 Level 1 下按 **Level 2/3** 分组（如 "Data Governance"）
- ✅ 点击分组标题可以**折叠/展开**
- ✅ 显示每个分组的**风险数量**

### Risk Report（主内容区）
- ✅ 按 **Level 1** 分组显示
- ✅ 每个 Level 1 下按 **Level 2/3** 分组
- ✅ 点击分组标题可以**折叠/展开**
- ✅ 风险卡片在对应的分组下显示

---

## 🐛 如果还是看不到

### 检查数据字段

在控制台输入：

```javascript
// 查看当前 paper 数据
const papers = JSON.parse(localStorage.getItem('riskgen_papers'))
console.log('Papers:', papers)
console.log('First paper results:', papers[0]?.results)

// 检查是否有新字段
papers[0]?.results?.forEach(r => {
    console.log('Risk:', {
        id: r.id,
        level1_risk_type: r.level1_risk_type,
        level2_3_risk_category: r.level2_3_risk_category,
        has_both_fields: !!(r.level1_risk_type && r.level2_3_risk_category)
    })
})
```

### 如果字段缺失

说明数据没有正确更新，手动清除并刷新：

```javascript
localStorage.removeItem('riskgen_papers')
location.reload()
```

---

## 💡 提示

- 清除数据**不会影响项目数据**（projects），只会清除风险报告（papers）
- 清除后，系统会自动从 `js/demo_data.js` 重新加载数据
- 新数据包含 `level1_risk_type` 和 `level2_3_risk_category` 字段

---

**清除数据后，刷新页面即可看到新的两级分组功能！** 🎉

